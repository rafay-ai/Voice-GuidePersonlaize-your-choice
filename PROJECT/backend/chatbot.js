// This is your chatbot's brain
const apiKey = 'YOUR_API_KEY_HERE';

function askChatbot(userMessage) {
    // This will talk to Google's AI
    // We'll add the full code later
    return "Hello! I'm learning about your preferences!";
}